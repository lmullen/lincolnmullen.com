{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ .Site.Params.blogFeedTitle }}</title>
    <link>{{ .Permalink | absURL }}</link>
    <description>Recent blog posts by {{ .Site.Author.name }}</description>
    <generator>Hugo -- gohugo.io</generator>
    {{ with .Site.LanguageCode }}
    <language>{{.}}</language>
    {{end}}
    {{ with .Site.Author.email }}<managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor> {{end}}
    {{ with .Site.Author.email }}<webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}
    {{ with .Site.Copyright }}<copyright>{{.}}</copyright>{{end}}
    {{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{ with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{ end }}
    {{ range first 25 (where .Site.RegularPages "Section" "in" (slice "blog" "links")) }}
    <item>
      {{ if .Params.Linkpost }}
      <title>Link post: {{ .Content | plainify | truncate 75 | safeHTML }}</title>
      <link>{{ .Params.Linkpost | absURL | htmlEscape }}</link>
      {{ else }}
      <title>{{ .Title | safeHTML }}</title>
      <link>{{ .Permalink | absURL | htmlEscape }}</link>
      {{ end }}
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}
      {{ if .Params.Linkpost }}
      <guid>{{ .Permalink | absURL }}</guid>
      {{ else }}
      <guid isPermaLink="true">{{ .Permalink | absURL }}</guid>
      {{ end }}
      <description>{{ .Content | html }}</description>
    </item>
    {{ end }}
  </channel>
</rss>
